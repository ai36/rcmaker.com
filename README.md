# React Component Maker

Marketing and documentation site for the [React Component Maker](https://marketplace.visualstudio.com/items?itemName=ai36.react-component-maker) VS Code extension.

[Live site](https://rcmaker.com) · [VS Code Marketplace](https://marketplace.visualstudio.com/items?itemName=ai36.react-component-maker) · [GitHub](https://github.com/ai36/react-component-maker)

## Tech Stack

- **Next.js 16** — App Router, server components, static generation
- **React 19** — UI
- **TypeScript** — strict mode
- **Tailwind CSS v4** — styling
- **next-intl v4** — i18n (EN / RU), `localePrefix: 'as-needed'`
- **next-themes** — light / dark / system theme
- **react-markdown** + rehype/remark plugins — markdown rendering for docs pages
- **shadcn/ui** — base UI components (button, badge, scroll-area, separator)
- **@next/third-parties** — Google Analytics (GA4) via `<GoogleAnalytics>`

## Project Structure

```
app/
├── layout.tsx              # Root layout (fonts, favicon, GA consent script, GoogleAnalytics)
├── robots.ts               # /robots.txt
├── sitemap.ts              # /sitemap.xml with hreflang alternates
└── [locale]/
    ├── layout.tsx          # Locale layout (header, theme, i18n provider, ConsentBanner)
    ├── page.tsx            # Home page
    ├── docs/page.tsx       # Documentation
    ├── changelog/page.tsx  # Changelog
    └── roadmap/page.tsx    # Roadmap

components/
├── layout/
│   ├── SiteHeader.tsx      # Adaptive header with mobile drawer, locale selector, theme toggle
│   ├── SiteFooter.tsx
│   ├── Sidebar.tsx         # Docs sidebar with anchor navigation (hash-based IDs)
│   └── DocLayout.tsx       # Docs page layout (sidebar + content, max-w-[1500px])
├── ui/                     # shadcn/ui primitives (button, badge, scroll-area, separator)
├── ConsentBanner.tsx       # Cookie consent banner (updates GA Consent Mode v2)
├── HeroBeams.tsx           # Animated hero background (CSS rotation + rAF float, IntersectionObserver)
├── LocaleSelector.tsx      # EN / RU language switcher
├── ThemeToggle.tsx         # Light / dark / system toggle
├── ThemeProvider.tsx       # next-themes provider
└── developer/Developer.tsx # Dev-only tooling overlay

content/
├── en/                     # English markdown content
│   ├── README.md           # Docs
│   ├── CHANGELOG.md
│   └── SOON.md             # Roadmap
└── ru/                     # Russian markdown content

messages/
├── en.json                 # English UI strings
└── ru.json                 # Russian UI strings

lib/
├── consent.ts              # Cookie helpers (readCookie, writeCookie, CONSENT_COOKIE)
├── metadata.ts             # pageAlternates(), ogLocale() — shared SEO helpers
├── slugHash.ts             # djb2 hash for heading anchor IDs (Unicode-safe)
├── markdown.ts             # readMarkdownFile()
├── button-variants.ts      # CVA button variant definitions
└── utils.ts                # cn()

constants/
└── index.ts                # PROJECT_NAME, SITE_URL, NAV_ITEMS, SIDEBAR_ITEMS, EXTERNAL_LINKS

i18n/
├── routing.ts              # next-intl routing config
├── request.ts              # per-request locale resolution
└── navigation.ts           # locale-aware Link, useRouter, usePathname

proxy.ts                    # next-intl middleware (localePrefix, routing)
```

## Localization

The site supports English (default) and Russian via `next-intl` with `localePrefix: 'as-needed'`:

| Locale | URL prefix | Example |
|--------|-----------|---------|
| EN | *(none)* | `https://rcmaker.com/docs` |
| RU | `/ru` | `https://rcmaker.com/ru/docs` |

Translations live in `messages/en.json` and `messages/ru.json`.
Markdown content lives in `content/en/` and `content/ru/`.

## Docs Sidebar Navigation

Sidebar items in `constants/index.ts` use `headingText: { en, ru }` with the exact heading text from the markdown files. The `slugHash()` function (djb2) computes a stable anchor ID from the heading text — the same function is used in both the rehype plugin (sets `id` on headings) and the sidebar (builds `href`). This works for any Unicode text including Cyrillic.

## SEO

Each page exports `generateMetadata` with:
- Localized `title` and `description`
- `alternates.canonical` — locale-specific canonical URL
- `alternates.languages` — hreflang for EN, RU, and `x-default`
- `openGraph.locale` — `en_US` or `ru_RU`
- Twitter card (`summary_large_image`) — from locale layout base metadata

`/robots.txt` and `/sitemap.xml` are generated by `app/robots.ts` and `app/sitemap.ts`.

## Google Analytics & Cookie Consent

GA4 is loaded via `<GoogleAnalytics gaId={GA_ID} />` from `@next/third-parties/google`.

Consent Mode v2 defaults are set via an inline `<script>` in `<head>` (must run before gtag.js loads):

```js
window.dataLayer.push(['consent', 'default', { analytics_storage: 'granted', wait_for_update: 1500 }])
```

`ConsentBanner` reads the consent cookie on mount. If no preference is stored it shows the banner. Accept/Decline call `window.gtag('consent', 'update', ...)` and persist the choice to a cookie named `{project_name}_analytics` (1 year, `SameSite=Lax`).

## Development

```bash
npm run dev     # start dev server at http://localhost:3000
npm run build   # production build
npm run start   # start production server
npm run lint    # ESLint
```

## Environment Variables

| Variable | Description |
|----------|-------------|
| `NEXT_PUBLIC_PROJECT_NAME` | Site/product name (default: `RC Maker`) |
| `NEXT_PUBLIC_GA_ID` | Google Analytics measurement ID (GA4) |
| `NEXT_PUBLIC_MARKETPLACE_URL` | VS Code Marketplace URL override |
| `NEXT_PUBLIC_GITHUB_URL` | GitHub repository URL override |
| `NEXT_PUBLIC_API_URL` | API base URL (reserved, not used) |
